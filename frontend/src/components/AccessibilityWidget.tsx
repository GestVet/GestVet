import 'sienna-accessibility'

/**
 * Widget de accesibilidad Sienna (MIT, github.com/bennyluk/Sienna-Accessibility-Widget).
 * El paquete se auto-inicializa como side-effect al importarse — corre
 * enteramente en el navegador del visitante, sin llamar a ningún servidor
 * externo ni requerir cuenta, a diferencia de UserWay.
 */
export default function AccessibilityWidget() {
  return null
}
